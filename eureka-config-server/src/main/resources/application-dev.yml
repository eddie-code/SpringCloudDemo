server:
  port: 8888
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
    fetch-registry: false
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/eddie-code/SpringCloudDemo
          search-paths: pigeonhole
          username:
          passphrase:
      label: master